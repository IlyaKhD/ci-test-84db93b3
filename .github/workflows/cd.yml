name: CD

on:
  push:
    branches:
      - releases/*
    paths:
      - package.json

jobs:

  hello:

    runs-on: windows-latest
    name: Hello
    
    steps:
      - name: print
        run: echo hello